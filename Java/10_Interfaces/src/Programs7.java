//an interface is declared using the interface keyword
public interface Programs7 {

	//all the fields are public, static and final by default
	//creating a instance variable
	int val = 11;
	
	//creating a abstract method. abstract method does not have a body, it used to be public and abstract
	void myMethod();
}
